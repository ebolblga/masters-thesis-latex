\section{Реализация сервиса для программной поддержки генерации расписания учебных заведений}

\subsection{Заполнение базы данных}

В идеальном сценарии для заполнения базы данных можно использовать API систем университета, хранящих такие данные как списки групп, преподавателей и учебные планы. Однако на момент написания данной выпускной квалификационной работы такая возможность отсутствует.

Альтернативный вариант --- производить парсинг PDF файлов учебных планов и календарных учебных графиков, которые находятся в открытом доступе на сайте университета \cite{stankin:edu_programs,stankin:calendar}.

Однако существует третий вариант: переиспользовать написанный автором данной выпускной квалификационной работы в бакалавриате парсер \cite{github:grad_work_alpha} PDF файлов расписаний учебных занятий. Хоть это и не будет финальным решением, готовым для запуска системы в работу на пользу учебного заведения --- это будет путём наименьшего сопротивления.

Этот подход также решит две другие проблемы, не имеющие элегантного решения. Тогда, как списки групп можно получить из соответствующего форума в ЭОС ФГБОУ ВО «МГТУ «СТАНКИН» \cite{stankin:groups}, актуальные списки преподавательского состава или таблицы со всеми учебными аудиториями и оборудованием найти не получится. В случае получения данных сразу из готового расписания за прошлый семестр можно определить и какие предметы ведут какие преподаватели, и какие предметы проводятся в каких аудиториях.

\subsubsection{Процесс популяции базы данных}

Скачать все расписания на текущий семестр можно в соответствующем форуме в ЭОС ФГБОУ ВО «МГТУ «СТАНКИН» \cite{stankin:schedule}. Тамже можно найти и списки групп.

Для заполнения базы данных требуемыми данными была написана специальная вспомогательная программа \cite{github:subject_parser}, производящая парсинг данных и приводящая их в нужную структуру и формат в SQLite (см. \figref{fig:db}).

\begin{figure}[H]
	\centering
	\includegraphics[width=1\textwidth]{images/ch5/db.png}
	\caption{Заполненная БД}
	\label{fig:db}
\end{figure}

\subsection{Сервер}

Как уже описывалось в предыдущей главе, в качестве основы вычислительной части проекта был выбран Polars. На основе его декларативного языка программирования был реализован алгоритм решения задачи.
Все основные методы были обёрнуты в REST API благодаря FastAPI, а Swagger позволил упростить процесс написания будущего клиента.

Также были разработаны дополнительные инструменты визуализации расписания для упрощения дебага текущего состояния расписания, как например интерактивная визуализация всего расписания на семестр (см. \figref{fig:debug}).

\begin{figure}[H]
	\centering
	\includegraphics[width=1\textwidth]{images/ch5/debug.png}
	\caption{Интерактивная визуализация расписания университета на семестр}
	\label{fig:debug}
\end{figure}

\subsubsection{Реализация WFC на языке Polars}

Первым шагом является генерация первичного расписания, удовлетворяющего минимальным требованиям к расписанию учебных занятий. Этот шаг выполняется методом WFC.

Детальное описание алгоритма приведено ниже.

\paragraph{Подготовка таблиц}

\begin{enumerate}
	\item Загрузка из БД множеств групп $G$, преподавателей $L$, дисциплин $S$,
	а также соответствий «группа--дисциплина» ($\mathrm{SubjectGroups}$) и
	«преподаватель--дисциплина» ($\mathrm{SubjectLecturers}$).

	\item Отфильтровка дисциплин без назначенного преподавателя.
	После этапа парсинга расписаний встречались предметы без преподавателей,
	и такие случаи требуется проработать в индивидуальном порядке.

	\item Построение множества рабочих дней $D$:
	от начала до конца семестра согласно учебному плану, без воскресений.

	\item Задание числа пар в день $T=8$ и нумерация слотов
	$t \in \{0,\ldots,T-1\}$.

	\item Формирование \textbf{суперпозиции} --- декартова произведения:
	\[
	\mathcal{C}_G = G \times D \times \{0,\ldots,T-1\}, \qquad
	\mathcal{C}_L = L \times D \times \{0,\ldots,T-1\}.
	\]
	Каждая ячейка $(g,d,t)$ или $(\ell,d,t)$ изначально свободна
	($\texttt{subject\_id}=\varnothing$).

	\item Вычисление \textbf{веса ячейки} $w(g,d,t)$:
	\[
	w(g,d,t)=\log\!\bigl(1+w_{\mathrm{day}}(\mathrm{weekday}(d))\bigr)\cdot
	w_{\mathrm{slot}}(t),
	\]
	с обнулением в праздники; для занятых ячеек $w(g,d,t)=0$. Именно на этом шаге учитываются предпочтения преподавателей по нагрузке по дням недели, а для студентов двигается распределение --- дневная или вечерняя форма обучения (см. \figref{fig:matrix}).

	\begin{figure}[H]
		\centering
		\includegraphics[width=1\textwidth]{images/ch5/weight.png}
		\caption{Трёхмерная поверхностная диаграмма распределения весов по временным слотам и группам}
		\label{fig:weight}
	\end{figure}

	\item Вычисление \textbf{энтропии} группы $H(g)$ --- суммарного числа оставшихся
	занятий по всем дисциплинам группы (см. \figref{fig:matrix}):
	\[
	H(g)=\sum_{s\in S(g)}
	\bigl(n^{\mathrm{лек}}_{g,s}+n^{\mathrm{сем}}_{g,s}+n^{\mathrm{лаб}}_{g,s}\bigr).
	\]

	\begin{figure}[H]
		\centering
		\includegraphics[width=1\textwidth]{images/ch5/entropy.png}
		\caption{Трёхмерная поверхностная диаграмма распределения энтропии по временным слотам и группам}
		\label{fig:entropy}
	\end{figure}

	Аналогично определяется $H(\ell)$ для преподавателей.
\end{enumerate}

\paragraph{Итерация WFC (один шаг \texttt{apply\_one\_step})}

Повторять, пока
\[
\sum_{g\in G}\sum_{s\in S(g)}
\bigl(n^{\mathrm{лек}}_{g,s}+n^{\mathrm{сем}}_{g,s}+n^{\mathrm{лаб}}_{g,s}\bigr)>0.
\]

\begin{enumerate}
	\item Выбор ячейки для коллапса.
	Среди свободных ячеек из $\mathcal{C}_G$ с $H(g)>0$ выбирается множество
	с минимальной энтропией:
	\[
	\mathcal{M}=\arg\min_{(g,d,t)\in\mathcal{C}_G,\;H(g)>0} H(g).
	\]
	Затем случайно выбирается $(g^{*}, d^{*}, t^{*}) \in \mathcal{M}$
	с вероятностями, пропорциональными $\exp(w-\max w)$
	(softmax по весу).

	\item Сужение по преподавателям.
	\[
	L^{*}=\{\ell \in L : \exists s \in S(g^{*}),\; (\ell,s)\in\mathrm{SubjectLecturers}\},
	\]
	после чего из этого множества исключаются преподаватели, уже занятые
	в слоте $(d^{*}, t^{*})$:
	\[
	L^{*}\leftarrow \{\ell \in L^{*} : \text{ячейка }(\ell,d^{*},t^{*})\text{ свободна}\}.
	\]

	\item Коллапс дисциплины и типа занятия.
	Для каждой пары $(s,c)$, где $c\in\{\mathrm{лек},\mathrm{сем},\mathrm{лаб}\}$,
	для которой $n^c_{g^{*},s}>0$ и существует $\ell\in L^{*}$ такой, что
	$(\ell,s)\in\mathrm{SubjectLecturers}$, выбирается одна тройка
	$(s^{*},c^{*},\ell^{*})$ взвешенным случайным выбором.
	Вес кандидата задаётся как
	\[
	p(\ell)\propto \frac{1}{H(\ell)+\varepsilon},
	\qquad \varepsilon=10^{-6}.
	\]

	\item Определение целевых групп.
	\[
	G_{\mathrm{tar}}=
	\begin{cases}
		\{g^{*}\}, & c^{*}\in\{\mathrm{сем},\mathrm{лаб}\},\\[4pt]
		\{g\in G : \text{группа }g\text{ относится к тому же потоку, что и }g^{*}\}, &
		c^{*}=\mathrm{лек},
	\end{cases}
	\]
	где «поток» --- это группа групп с общим префиксом имени
	(например, \texttt{ИДМ-24-} для \texttt{ИДМ-24-08}),
	пересечённая с $S(g)$ для выбранной дисциплины $s^{*}$.

	\item Назначение (коллапс).
	Для всех $g\in G_{\mathrm{tar}}$ в ячейку $(g,d^{*},t^{*})$ записывается:
	\[
	(s^{*}, c^{*}, \ell^{*}), \qquad
	\texttt{subject\_type}\in\{0,1,2\}\Leftrightarrow\{\mathrm{лек},\mathrm{сем},\mathrm{лаб}\}.
	\]
	После этого заносится занятость в ячейку преподавателя $(\ell^{*},d^{*},t^{*})$.

	\item Локальное распространение ограничений.
	\begin{itemize}
		\item Уменьшить $n^{c^{*}}_{g,s^{*}}$ на 1 для всех $g\in G_{\mathrm{tar}}$;
		\item Уменьшить $H(g)\leftarrow\max(0,H(g)-1)$ для всех $g\in G_{\mathrm{tar}}$;
		\item Обнулить $w$ для занятых ячеек.
	\end{itemize}

	\item Недельное распространение (flood fill).
	Пока
	\[
	\min_{g\in G_{\mathrm{tar}}} n^{c^{*}}_{g,s^{*}}>0,
	\]
	для $k=1,2,\ldots,52$ и $\sigma\in\{+1,-1\}$ выполняется попытка
	поставить то же занятие в $(g,d^{*}+\sigma k\text{ нед.},t^{*})$
	для всех $g\in G_{\mathrm{tar}}$, если:
	\begin{itemize}
		\item дата находится в пределах $[d_{\min},d_{\max}]$;
		\item ячейки групп свободны ($\texttt{subject\_id}=\varnothing$);
		\item преподаватель $\ell^{*}$ свободен в этом слоте.
	\end{itemize}
	При успешном назначении повторяется п.~6:
	декрементируются счётчики и обновляется энтропия.
\end{enumerate}

\paragraph{Завершение WFC}

\begin{enumerate}
	\item Остановка происходит, когда все счётчики
	$n^{\mathrm{лек}}$, $n^{\mathrm{сем}}$, $n^{\mathrm{лаб}}$
	обнуляются.
	
	\item $\mathcal{C}_G$ и $\mathcal{C}_L$ сохраняются как первичное
	расписание; поле \texttt{classroom\_id} остаётся пустым
	($\varnothing$) и заполняется на отдельном этапе после
	генетической оптимизации (см.~ниже).
\end{enumerate}

\subsubsection{Реализация генетического алгоритма на языке Polars}

Вторым шагом является улучшение первичного расписания, полученного
методом WFC, с помощью эволюционной стратегии.
Кроссовер не используется: эволюция строится на отборе,
мутациях, сохраняющих допустимость решения, и локальном поиске.

\paragraph{Функция приспособленности}

Приспособленность задаётся как
\[
F(X)=W_{\mathrm{soft}}\cdot S(X)-P_{\mathrm{hard}}\cdot V_{\mathrm{hard}}(X),
\]
где $W_{\mathrm{soft}}=10^3$, $P_{\mathrm{hard}}=10^6$,
\[
S(X)=
w_{\mathrm{win}}\,s_{\mathrm{win}}(X)+
w_{\mathrm{load}}\,s_{\mathrm{load}}(X)+
w_{\mathrm{start}}\,s_{\mathrm{start}}(X)+
w_{\mathrm{pat}}\,s_{\mathrm{pat}}(X),
\]
все веса $w_*=1$.

\subparagraph{Жёсткие ограничения ($V_{\mathrm{hard}}$).}
Суммарное число нарушений:
\begin{itemize}
	\item \textbf{учебный план} --- расхождение фактического и ожидаемого
	числа пар по $(g,s,c)$, а также «лишние» занятия вне плана;
	\item \textbf{календарь} --- занятия вне $D$ или в праздники $H$;
	\item \textbf{слоты} --- $t\notin\{0,\ldots,T-1\}$;
	\item \textbf{конфликты групп} --- более одного занятия в
	$(g,d,t)$;
	\item \textbf{конфликты преподавателей} --- более одного \emph{различного}
	занятия в $(\ell,d,t)$; допускается совместное ведение одной лекции
	в потоке (одинаковые $s$ и $c$);
	\item \textbf{назначение преподавателя} --- пара $(\ell,s)\notin\mathrm{subject_lecturers}$
	или $\ell=\varnothing$.
\end{itemize}
Решение \textbf{допустимо}, если $V_{\mathrm{hard}}(X)=0$.

\subparagraph{Мягкие критерии ($S$).}
Каждый критерий нормируется в $(0,1]$ по формуле $1/(1+\text{метрика})$:
\begin{enumerate}
	\item \textbf{Окна} ($s_{\mathrm{win}}$).
	Для каждой пары $(g,d)$ считается число «пустых» слотов между
	первой и последней парой дня:
	\[
	W_{\mathrm{tot}}=
	\sum_{g,d}\Bigl(
	\max_{t\in X(g,d)} t-\min_{t\in X(g,d)} t+1-|X(g,d)|
	\Bigr).
	\]
	
	\item \textbf{Равномерность нагрузки по дням недели} ($s_{\mathrm{load}}$).
	Для группы $g$ вычисляется коэффициент вариации числа пар
	по дням недели; затем усредняется по группам.
	
	\item \textbf{Стабильность времени начала} ($s_{\mathrm{start}}$).
	Для каждой тройки $(g,s,c)$ берётся стандартное отклонение слота $t$
	по всем неделям; затем усредняется.
	
	\item \textbf{Недельный паттерн} ($s_{\mathrm{pat}}$).
	Для $(g,s,c,\mathrm{weekday}(d))$ штрафуется использование
	более одного различного слота:
	\[
	P_{\mathrm{pat}}=
	\sum \max\bigl(|\{t\}|-1,\,0\bigr).
	\]
\end{enumerate}

\paragraph{Операторы мутации}

Все операторы перемещают одно занятие из ячейки $(g,d,t)$ в
$(g,d',t')$ при выполнении предиката $\mathrm{can\_place}$:
\begin{itemize}
	\item $d'\in D\setminus H$;
	\item целевая ячейка группы свободна;
	\item преподаватель $\ell$ свободен в $(d',t')$ либо ведёт то же
	$(s,c)$ (потоковая лекция);
	\item $(\ell,s)\in\mathrm{subject_lecturers}$.
\end{itemize}

Вариации алгоритма SA:
\begin{enumerate}
	\item \texttt{holiday\_repair} --- перенос занятия с праздничной даты
	на случайный рабочий день и слот.
	
	\item \texttt{relocate\_session} --- случайный перенос произвольного
	назначенного занятия.
	
	\item \texttt{compress\_day} --- выбор дня $(g,d)$ с окнами
	($\max t-\min t+1-|\{t\}|>0$), выбор пустого слота внутри
	дневного интервала и сдвиг крайнего занятия в этот слот.
	
	\item \texttt{align\_pattern\_slot} --- для «размазанного» паттерна
	$(g,s,c,\mathrm{weekday})$ выбирается целевой слот (медиана по
	текущим слотам) и одно занятие переносится на него в одном из
	соответствующих рабочих дней.
\end{enumerate}

Случайный оператор выбирается равновероятно; при наличии
праздничных занятий с вероятностью $0{,}5$ приоритетно вызывается
\texttt{holiday\_repair}. Каждая попытка --- до $40$ случайных
кандидатов $(d',t')$.

\paragraph{Инициализация популяции}

Размер популяции $N_{\mathrm{pop}}$.
\begin{enumerate}
	\item Элита: seed-расписание WFC без изменений.
	\item Остальные $N_{\mathrm{pop}}-1$ особи: копии seed с
	$120$ случайными мутациями (с приоритетом \texttt{holiday\_repair}).
	\item Каждая особь оценивается функцией $F$; популяция сортируется
	по убыванию приспособленности.
\end{enumerate}

\paragraph{Итерация эволюции}

Повторять до $G_{\max}$ поколений или до плато без улучшения лучшего решения.

\begin{enumerate}
	\item \textbf{Элитизм}: лучшие особи переносятся в следующее поколение без изменений.
	
	\item \textbf{Отбор родителя}: турнирный отбор ($k=3$) ---
	из $k$ случайных особей выбирается лучшая по $F$.
	
	\item \textbf{Потомок}: клон родителя; к нему применяется
	$2$ случайные мутации.
	
	\item \textbf{Локальный поиск} (hill climbing): до $30$ попыток
	случайной мутации; принимается только улучшение или сохранение $F$.
	Это ускоряет сходимость без нарушения допустимости.
	
	\item \textbf{Формирование нового поколения}: все потомки оцениваются,
	популяция снова сортируется по $F$.
	
	\item \textbf{Глобальный лучший}: если $F$ лучшей особи поколения
	превышает текущий рекорд, обновляется $\texttt{best}$; иначе
	счётчик плато увеличивается на~1.
\end{enumerate}

\paragraph{Завершение генетического алгоритма}

\begin{enumerate}
	\item Остановка по достижению плато или лимита поколений.
	
	\item Сохранение лучшего расписания (см. \figref{fig:evolution}).
\end{enumerate}

\begin{figure}[H]
	\centering
	\includegraphics[width=1\textwidth]{images/ch5/evolution.png}
	\caption{Результат генетического алгоритма после 1000 поколений}
	\label{fig:evolution}
\end{figure}

\subsubsection{Назначение аудиторий}

Третьим шагом к уже оптимизированному расписанию групп
(после WFC и генетического алгоритма) последовательно
назначаются аудитории. Этот этап не участвует в эволюции:
временная структура расписания фиксирована, меняется только
поле \texttt{classroom\_id}.

\paragraph{Подготовка}

\begin{enumerate}
	\item Загрузка множества аудиторий $A$ с атрибутами
	$\mathrm{cap}(a)$ (вместимость) и $\mathrm{equip}(a)$ (оборудование).
	
	\item Предварительное вычисление матрицы кратчайших расстояний
	$\mathrm{Dist}(a_i,a_j)$ между аудиториями
	(см.~\figref{fig:matrix}).
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=1\textwidth]{images/ch5/matrix.png}
		\caption{Матрица примерных расстояний между аудиториями}
		\label{fig:matrix}
	\end{figure}
\end{enumerate}

\paragraph{Назначение по занятиям}

Для каждого назначенного занятия $(g,d,t,s,c,\ell)$ в порядке
обхода расписания строится множество допустимых аудиторий:
\[
A_{\mathrm{adm}}(g,d,t,s,c)=
\bigl\{
a \in A :
\mathrm{cap}(a)\ge |G_{\mathrm{tar}}|,\;
\mathrm{equip}(a)\supseteq \mathrm{lab\_equipment}(s,c),\;
a \text{ свободна в слоте } (d,t)
\bigr\},
\]
где $G_{\mathrm{tar}}$ --- целевые группы занятия
(одна группа для семинара/лабораторной, поток --- для лекции).

Выбор аудитории выполняется жадным образом с учётом уже
назначенных занятий группы $g$ в тот же день $d$:

\begin{itemize}
	\item если для дня $d$ это первое занятие группы $g$,
	выбирается первая аудитория из $A_{\mathrm{adm}}$, отсортированного
	в лексикографическом порядке по номеру аудитории;
	
	\item если в этот день уже назначена аудитория $a_{\mathrm{prev}}$,
	множество $A_{\mathrm{adm}}$ сортируется по возрастанию
	$\mathrm{Dist}(a_{\mathrm{prev}}, a)$, и выбирается первая
	допустимая аудитория.
\end{itemize}

Так достигается двойная цель: минимизация числа используемых
аудиторий за счёт закрепления кабинетов за группами и
минимизация перемещений внутри корпуса за счёт локальной
оптимизации переходов между последовательными парами.

\subsection{Клиент}

В роли клиента было написано веб-приложение на базе Nuxt 4 и Vue 3.5 (см. \figref{fig:client}), а технология PWA позволяет установить сайт как нативное приложение на любое современное устройство, поддерживающее браузер. То есть генерацию расписания можно производить даже с некоторых холодильников или наручных часов.

\begin{figure}[H]
	\centering
	\includegraphics[width=1\textwidth]{images/ch5/client.png}
	\caption{Клиент}
	\label{fig:client}
\end{figure}

\subsection{Вывод по пятой главе}

Была реализована программная основа сервиса для генерации расписания учебных занятий. Для системы была реализована трёхзвенная архитектура, обеспечивающая разделение ответственности между хранилищем данных, серверной логикой и UI. Был подготовлен механизм заполнения БД на основе доступных источников данных.

На стороне сервера реализована бизнес-логика и REST API для взаимодействия с клиентской частью и внешними компонентами системы. На стороне клиента разработано веб-приложение, позволяющее удобно работать с сервисом через браузер. В результате получена работоспособная система, пригодная для дальнейшего развития и последующей интеграции в более полный процесс автоматизированного формирования расписания.
